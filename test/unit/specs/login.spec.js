import Vue from 'vue'
import Login from '@/pages/login'

describe('pages', () => {
  describe('login.vue', () => {
    it('渲染正确', () => {
      // const Constructor = Vue.extend(Login)
      // const vm = new Constructor()
      // vm.$mount()
      // expect(vm.$el.textContent.trim()).to.equal('login')
    })
    it('跳转/', () => {})
    describe('验证码按钮', () => {
      it('可点击', () => {})
      it('不可点击', () => {})
    })
    describe('登录按钮', () => {
      it('可点击', () => {})
      it('不可点击', () => {})
    })
  })
})
